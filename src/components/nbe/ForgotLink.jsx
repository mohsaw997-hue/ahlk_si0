export default function ForgotLink({ children }) {
  return (
    <button
      type="button"
      className="text-sm text-zinc-800 underline text-start  w-full my-5"
    >
      {children}
    </button>
  );
}
